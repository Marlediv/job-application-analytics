[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "job-application-analytics"
version = "0.1.0"
description = "Analytics-Projekt fuer Bewerbungsdaten"
requires-python = ">=3.10"
